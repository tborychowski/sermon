// const {logger} = require('./lib');
// const {readJsonFile, writeJsonFile} = require('./lib');


function alerter (data) {
	// 1. check constraints
	// 2. build notification string
	// 3. send!
}


module.exports = alerter;
